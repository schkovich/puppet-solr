<solr persistent="true" sharedLib="lib">
 <cores adminPath="/admin/cores">
  <% @cores.each do |core| %>
    <core name="<%= core -%>" instanceDir="<%= core -%>"
          dataDir="<%= data_dir -%>/<%= core -%>" />
  <% end %>
 </cores>
</solr>
